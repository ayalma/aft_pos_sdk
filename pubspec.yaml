name: aft_pos_sdk
description: The library allows to print receipts using an ESC/POS thermal Bluetooth printer.
version: 0.2.8
homepage: https://github.com/andrey-ushakov/esc_pos_bluetooth

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  commons:
    git: https://github.com/ayalma/esc_pos_commons.git


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: